pipeline {
    agent any

    stages {

        stage('Get Source') {
            steps {
                echo 'Getting source code'
            }
        }

        stage('Build') {
            steps {
                echo 'Build success'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy success'
            }
        }

    }
}